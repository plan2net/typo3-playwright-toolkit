rootPageId: 1
# Not the testing hostname: the site has to resolve on the ordinary one too, which
# is what the off-origin test navigates to.
base: '/'
websiteTitle: 'Playwright toolkit e2e'
# No site set: they are 13.4 and newer, and the TypoScript this fixture needs comes
# from the sys_template record every supported core reads.
languages:
  - languageId: 0
    title: English
    navigationTitle: English
    base: '/'
    locale: en_US.UTF-8
    flag: gb
